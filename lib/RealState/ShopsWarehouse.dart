import 'package:flutter/material.dart';

class ShopsWarehouse extends StatelessWidget {
  const ShopsWarehouse({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}