import 'package:flutter/material.dart';

class OfficeSpace extends StatelessWidget {
  const OfficeSpace({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}